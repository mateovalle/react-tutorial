import {LOGIN_SUCCESS, LOGOUT_REQUEST} from "../actions/sessionActions";

const initialState = {
    username: ''
}

const sessionReducer = (state= initialState, action) => {
    switch (action.type) {

    }
}

export default sessionReducer